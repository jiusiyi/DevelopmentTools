<UserControl x:Class="ControlEase.IoDrive.Hikvision.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<Label Grid.Row="0" Grid.Column="0" Content="{Binding AlarmResetTimeStr}" VerticalAlignment="Center" />
		<TextBox Text="{Binding AlarmResetTime,Mode=TwoWay}" Grid.Row="0" Grid.Column="1" MinWidth="100"  HorizontalAlignment="Stretch"  VerticalAlignment="Center"  Height="22" />
		<Label  Grid.Column="2" Grid.Row="0" Content="{DRes s}" HorizontalAlignment="Left"  VerticalAlignment="Center" />

	</Grid>
</UserControl>
